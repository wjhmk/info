<%= form_for([@post, @post.comments.build]) do |f| %>
	<%= f.label :名前 %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :body,"コメント" %><br>
	<%= f.text_area :body %><br>
	<br>
	<%= f.submit "コメント投稿"%>
<% end %>